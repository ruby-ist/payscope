<%= turbo_stream.replace "flash", partial: "shared/flash" %>

<%# Success returns the row to its display state; a failure keeps it in the form
    state so the errors stay visible. %>
<%= turbo_stream.replace dom_id(@exchange_rate), partial: "exchange_rates/rate_frame",
                         locals: { exchange_rate: @exchange_rate, editing: @exchange_rate.errors.any? } %>
